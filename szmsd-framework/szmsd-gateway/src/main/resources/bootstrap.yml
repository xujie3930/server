# Tomcat
server:
  port: 8080

# Spring
spring: 
  application:
    # 应用名称
    name: szmsd-gateway
  profiles:
    # 环境配置
    active: test
  cloud:
    nacos:
      discovery:
        # 服务注册地址
        server-addr: 183.3.221.229:22260
        namespace: d396cb84-b74c-498a-ab09-dd8571f8d737
      config:
        # 配置中心地址
        server-addr: 183.3.221.229:22260
        # 配置文件格式
        file-extension: yml
        # 共享配置
        shared-dataids: application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
        namespace: d396cb84-b74c-498a-ab09-dd8571f8d737
        group: DEFAULT_GROUP