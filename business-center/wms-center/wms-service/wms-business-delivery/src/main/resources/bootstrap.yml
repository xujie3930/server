# Tomcat
server:
  port: 7202

# Spring
spring:
  application:
    # 应用名称
    name: wms-business-delivery
  profiles:
    # 环境配置
    active: dev

com:
  szmsd:
    ck1:
      values:
        create: "/v1/outboundOrders"
        finished: "/v1/outboundOrders/{packageId}/finished"
        cancel: "/v1/outboundOrders/{packageId}/cancelState"
        "a": "/v1/ladingOrders"
        "b": "/v1/ladingOrders/{shipmentId}/cancelState"